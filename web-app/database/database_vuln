Deliberate vulnerabilities embedded in this schema are marked with:
--   [VULN-V7] MD5 password hashing (no salt)  → auth/auth.service.js
--   [VULN-V7] password_hash is VARCHAR(32) — MD5 hex output length.
--           bcrypt would require VARCHAR(60). The narrow column enforces
--           the weak hashing and prevents bcrypt from being stored later.

--   [VULN-V2] Unrestricted bio field           → profile/ProfilePage.jsx
--   [VULN-V2] bio is TEXT with no sanitization constraint.
--             The application renders bio as raw innerHTML, enabling stored XSS.

--   [VULN-V3] IDOR target — enrolment records fetched directly by primary id
--           in the API server with no ownership check.
--           Exploited via: GET /api/enrolments/:id
